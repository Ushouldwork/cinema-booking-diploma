import Header from '../../components/common/Header'

const LoginPage = () => (
  <div className="page page--admin">
    <Header admin />
    <main className="placeholder">
      <h1>Авторизация</h1>
      <p>Форма входа администратора будет подключена к API.</p>
    </main>
  </div>
)

export default LoginPage
